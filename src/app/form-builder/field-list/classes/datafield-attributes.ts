export class DatafieldAttributes {
  constructor(public id: string, public title: string, public placeholder: string, public value: string, public hint: string, public options?: Array<string>) {
  }
}
